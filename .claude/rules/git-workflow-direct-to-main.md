<!-- Synced by hand across the @vdaluz/astro-* package family - not generated or symlinked. -->

## Git workflow (small single-maintainer package)

No worktrees, no per-issue branches - work directly on `main`. This package is small, single-maintainer, and worked sequentially; consumers only ever see tagged releases, so `main` is safe to iterate on. This is a deliberate deviation from the portfolio's default branch-per-issue policy, not an oversight.
